import { SamplePublicationsPipe } from './sample-publications.pipe';

describe('SamplePublicationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SamplePublicationsPipe();
    expect(pipe).toBeTruthy();
  });
});
