import { ProtocolSearchResultsDisplayDescriptionPipe } from './protocol-search-results-display-description.pipe';

describe('ProtocolSearchResultsDisplayDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ProtocolSearchResultsDisplayDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
